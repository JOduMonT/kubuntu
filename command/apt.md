# apt

## Update database
`apt update`

## Upgrade all
`apt upgrade -y`

## Install
`apt install -y PACKAGENAME`

## Remove
> but keep the configuration file  
 
`apt remove -y PACKAGENAME`

## Purge
> and flush the configuration file  
 
`apt purge -y PACKAGENAME`

## List installed
`apt list --installed`
